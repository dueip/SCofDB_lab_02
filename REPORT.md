# Отчёт по лабораторной работе №2
## Управление конкурентными транзакциями в маркетплейсе

**Студент:** Воеводин Егор   
**Дата:** 16.04.26

---

## Раздел 1: Описание проблемы

### Что такое Race Condition?

Race condition (состояние гонки) — это ситуация, когда запускается несколько паралелльных операций, но при этом их результат зависит от порядка их выполнения. Изначально понятие Race Condition пошло из многопоточное программирования, где, если правильно не настраивать мьютексы и в целом цепочку вызывов, одна операция вполне могла перезаписать другую. 

**Пример из жизни:**
В многопоточном программировании:
Есть 2 функции, которые проводят разные операции над массивом (к примеру сначала добавляют в массив элемент, а потом вычитают из каждого элемента массива 2), а также есть функция, которая считает сумму массива. К примеру у нас есть массив \[1, 2, 3] Если запустить две функции с side-effect'ом в двух разных потоках, но при этом друг за другом, то может проихзойти две ситуации:
а) Все прошло хорошо и так как и планировалось: сначала сработала функция, которая добавила в массив элемент 4, тем самым сделав массив \[1, 2, 3, 4]. И потом вычли из каждого элемента массива 2, получил \[-1, 0, 1, 2]. В таком случае сумма элементов массива будет просто 2.
б) Все прошло не так, как планировалось: сначала сработала функци я, которая вычла 2-ку, тем самым массив превратился в \[-1, 0, 1], а потом добавила 4 (\[1, 2, 3, 4]). И сумма получилась 4. 
Главная проблема данной суитации в том, что неизвестно, как точно сработают данные функции, потому что каждый раз вызов потоков будет работать по-разному.

В базах данных:
В базах данных имеет место быть точно такой же пример, однако на уровне транзакций. Например, двойная оплата, когда пользователь успел два раза оплатить один и тот же order, хотя этого явно не должно было происходить.
- ...

### Почему READ COMMITTED не защищает от двойной оплаты?

_TODO: Объясните механизм работы READ COMMITTED и почему он не предотвращает race condition._

На уровне изоляции READ COMMITTED:
1. Каждая транзакция видит только закомиченные транзакции
2. Когда две транзакции проверяют отсутсие оплыты и не обнаружают ее они обе перейдут в ИНСЕРТ
3. В результате обе транзакции успешно закоммитятся, а READ COMMITED сработате вполнге правильно. 

**Демонстрация проблемы:**

```
Время | Сессия 1                    | Сессия 2
------|----------------------------|---------------------------
t1    | BEGIN                      |
t2    | SELECT status (created)    |
t3    |                            | BEGIN
t4    |                            | SELECT status (created)
t5    | pg_sleep(3)                |
t6    |                            | pg_sleep(3)
t7    | UPDATE status = paid       |
t8    |                            | UPDATE status = paid (ждет)
t9    | COMMIT                     |
t10   |                            | UPDATE выполняется...
t11   |                            | COMMIT
```
   - t1 Открывается транзакция на первой сессии
   - t2 Первая выбирает заказы со статусом "созданы"
   - t3 Вторая сессия начинает транзкацию
   - t4 ВТорая сессия выбирает заказы со статусом "созданы", а так как перпвая сессия не закоммитилась то вторая сесия нормально запустилась
   - t5 Первая сессия засыпает на 3 секунды. Имуляция реальной системы
   - t6 Аналогично, вторая сессия засыпает
   - t7 Первая сессия обновляет заказ и выставляет его как paid 
   - t8 Вторая сессия так же вызывает UPDATE для статуса, однако ждет изменения первой сессии. 
   - t9 Коммитит изменения и блокировка снимается
   - t10 Выполняется второй апдейт строки на Paid, срабатываеют все тригеры yada yada 
   - t11 Вторая сессия коммитит, обе транзакции завершились успешно  без ошибки

### Примеры из реальной жизни

_TODO: Приведите реальные примеры, когда может возникнуть такая проблема._

1. **Двойной клик на кнопку "Оплатить"**
   - Пользователь нажимает кнопку дважды
   - Два HTTP-запроса приходят почти одновременно
   - Оба запроса начинают обработку параллельно

2. **Микросервисная архитектура**
   - Два микросервиса одновременно получают инормацию об оплате заказа.
   - Оба микросервиса пытаются оплатить заказ

3. **Сбой сети**
   - Пользователь отправляте запрос на сервер
   - Сеть пользователя сбивается, обратный ответ не приходит и браузер пользователя не знает, что что-то произошло
   - Пользователь перезагружает страницу/браузер отправляет запрос повторно, тем самым вызывая двойную оплату. 

---

## Раздел 2: Уровни изоляции в PostgreSQL

### READ UNCOMMITTED

**Описание:**
_TODO: Опишите этот уровень изоляции._
Самый примитивный уровень изоляции. В данном случае транзакция может видеть результаты других транзакций, даже если они не закоммичены. 

**Предотвращает:**
- _TODO: Список аномалий, которые предотвращаются._

**Не предотвращает:**
- Dirty reads
- Non-repeatable reads
- Phantom reads

**Когда использовать:**
Лучше никогда, но может быть полезно в дебаге, аналитике, а также если очень хочется :)

**Особенность в PostgreSQL:**
В PostgreSQL READ UNCOMMITTED работает как READ COMMITTED из-за архитектуры MVCC.

---

### READ COMMITTED (по умолчанию)

**Описание:**
_TODO: Опишите этот уровень изоляции._

Каждый SELECT в транзакции видит только те изменения, которые в других параллельных транзакциях, которые были закомиченны. Полная противоположность READ UNCOMMITED в данном случае

**Предотвращает:**
- ✅ Dirty reads (чтение незакоммиченных данных)

**Не предотвращает:**
- ❌ Non-repeatable reads (повторное чтение дает другой результат)
- ❌ Phantom reads (новые строки появляются в результате запроса)

**Пример non-repeatable read:**
```sql
-- Сессия 1
BEGIN;
SELECT balance FROM accounts WHERE id = 1; -- Результат: 1000
-- Сессия 2 изменяет balance и делает COMMIT
SELECT balance FROM accounts WHERE id = 1; -- Результат: 500 (!)
COMMIT;
```

**Когда использовать:**
Это дефолтный параметр, так что он подходит для большинства случаев. Однако стоит помнить, что в данном случае остаются проблемы Non-repeatable reads и Phantom Reads/
---

### REPEATABLE READ

**Описание:**
_TODO: Опишите этот уровень изоляции._

Транзакция видит snapshot данных на момент начала транзакции. Тем самым никакая транзакция не может ни изменить, ни добавить, ни удалить строки.

**Предотвращает:**
- ✅ Dirty reads
- ✅ Non-repeatable reads
- ✅ Phantom reads (в PostgreSQL благодаря MVCC)

**Не предотвращает:**
- ❌ Write skew (специфичная аномалия)
- ❌ Некоторые сериализационные аномалии

**Особенность в PostgreSQL:**
В отличие от стандарта SQL, PostgreSQL на REPEATABLE READ также предотвращает phantom reads.

**Когда использовать:**
Из-за создания снапшота требует большего количества данных. В случае с MVCC также появляется сборщик мусора, т.е. условно отдельный пасс, который собирает ненужные старые версии данных. Это может плохо повлиять на перформанс, однако это все еще лучше SERIALIZABLE
---

### SERIALIZABLE

**Описание:**
_TODO: Опишите этот уровень изоляции._

Самый строгий уровень, гарантирующий полную блокировку любых действий, пока транзакция не будет завершена. 

**Предотвращает:**
- ✅ Все аномалии чтения
- ✅ Сериализационные аномалии
- ✅ Write skew

**Недостатки:**
- ❌ Может откатывать транзакции при конфликтах (serialization failure)
- ❌ Снижение производительности
- ❌ Требует retry logic в приложении

**Пример serialization failure:**
```sql
-- Сессия 1
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 100 WHERE id = 1;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 200 WHERE id = 2;

-- При COMMIT одна из транзакций получит ошибку:
-- ERROR: could not serialize access due to read/write dependencies
```

**Когда использовать:**
_TODO: В каких случаях этот уровень подходит._
Данный уровень подходит для транзакций, когда совершенно никогда нельзя позволить чему-то произойти несколько раз. Обычно такие вещи используются для финансовой транзакций. К примеру в некоторых играх время от времени появляется баг, когда за одну внутреигровую покупку может начислиться сразу несколько валюты. Достигается это, к примеру, снижением FPS'а до настолько маленьких цифр, что получаается нажать на кнопку покупки очень много раз. Тем самым, поскольку база данных не блокируется, начисляется кратно больше игровой валюты.
---

### Сравнительная таблица

| Уровень изоляции  | Dirty Read | Non-Repeatable Read | Phantom Read | Performance | Use Case |
|-------------------|------------|---------------------|--------------|-------------|----------|
| READ UNCOMMITTED  | ❌          | ❌                   | ❌            | Высокая     | Аналитика (неточная) |
| READ COMMITTED    | ✅          | ❌                   | ❌            | Высокая     | Обычные операции |
| REPEATABLE READ   | ✅          | ✅                   | ✅*           | Средняя     | Критичные операции |
| SERIALIZABLE      | ✅          | ✅                   | ✅            | Низкая      | Финансовые транзакции |

_*В PostgreSQL REPEATABLE READ также предотвращает phantom reads благодаря MVCC._

---

## Раздел 3: Решение проблемы

### Почему REPEATABLE READ решает проблему?

_TODO: Объясните, как REPEATABLE READ помогает решить проблему конкурентных оплат._

REPEATABLE READ использует snapshot isolation:
1. Транзакция видит снапшот на момент старта
2. Это означает, что повторное чтение произойдет стаибильно
3. Однако, без блокировок это не решит проблему конкуретной оплаты

### Зачем нужен FOR UPDATE?

_TODO: Объясните роль FOR UPDATE в решении проблемы._

`FOR UPDATE` создает эксклюзивную блокировку на уровне строки (row-level lock):

**Без FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...';
-- Другая транзакция может прочитать ту же строку
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**С FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...' FOR UPDATE;
-- Другая транзакция ЖДЕТ освобождения блокировки
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**Типы блокировок:**
- `FOR UPDATE` — эксклюзивная блокировка (никто не может изменить или заблокировать)
- `FOR SHARE` — разделяемая блокировка (другие могут читать с FOR SHARE, но не могут изменять)
- `FOR NO KEY UPDATE` — как FOR UPDATE, но разрешает concurrent FOR KEY SHARE
- `FOR KEY SHARE` — слабая блокировка (предотвращает DELETE и UPDATE ключевых полей)

### Что произойдет без FOR UPDATE на REPEATABLE READ?

_TODO: Опишите сценарий, когда REPEATABLE READ без FOR UPDATE не работает._

Даже на REPEATABLE READ возможна аномалия:

```sql
-- Сессия 1
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
COMMIT;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created (snapshot!)
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
-- ЧТО ПРОИЗОЙДЕТ?
```

_TODO: Объясните, что произойдет в этом случае._
В данном случае   вторая сессия попытается выполнить UPDATE, однако получит ошибку. Заказ останется оплаченным один раз, однако вторая транзакция все равно пыталась его оплатить.

### Разница между FOR UPDATE и FOR SHARE

| Характеристика | FOR UPDATE | FOR SHARE |
|----------------|------------|-----------|
| Тип блокировки | Эксклюзивная | Разделяемая |
| Блокирует чтение с FOR UPDATE | ✅ | ✅ |
| Блокирует чтение с FOR SHARE | ✅ | ❌ |
| Блокирует UPDATE | ✅ | ✅ |
| Блокирует DELETE | ✅ | ✅ |
| Use case | Перед изменением | Защита от изменений |

**Пример использования FOR SHARE:**
```sql
-- Проверка наличия товара перед созданием заказа
SELECT quantity FROM products WHERE id = '...' FOR SHARE;
-- Товар не может быть удален или изменен до COMMIT
```

---

## Раздел 4: Рекомендации для продакшена

### Какой ISOLATION LEVEL использовать для продакшена маркетплейса?

_TODO: Дайте обоснованную рекомендацию по выбору уровня изоляции для продакшена._

**Рекомендация:** Для продакшена маркетплейса рекомендуется использовать **READ COMMITTED как default уровень** с явным использованием **REPEATABLE READ + FOR UPDATE для критичных операций**.

#### Обоснование:

**1. Производительность**
- READ COMMITTED имеет минимальный overhead, так как не требует создания снапшотов или каких-то дополнительных ресурсов
- REPEATABLE READ используется только там, где необходимо удостовериться, что данные никто не изменит до коммита
- Избегаем глобального использования SERIALIZABLE, так как он привносит очень большой overhead, а лучше без оверхеда, чем с ним.

_TODO: Расширьте обоснование._

**2. Безопасность данных**
- Критичные операции (оплата, изменение баланса) защищены через REPEATABLE READ + FOR UPDATE, чтобы гарантировать защиту, но при этом дать минемальный оверхед
- Некритичные операции (просмотр каталога) работают на READ COMMITTED, потому что это простой SELECT -- ничего там поломаться критически не может, все в любом случае будет нормально
- Четкое разделение зон ответственности

_TODO: Расширьте обоснование._

**3. Риски deadlock**
- FOR UPDATE может привести к deadlock при неправильном порядке блокировок, к примеру если две трензакции пытаются заблокировать одну и ту же таблицу в разной последовательности
- Решение: всегда блокировать ресурсы в одном порядке
- Пример: сначала order, потом order_items, потом payment

_TODO: Расширьте обоснование._

**4. Простота разработки**
- READ COMMITTED легко понять и отлаживать, поэтому  в большинстве случаев по-дефолту стоит использовать его
- REPEATABLE READ + FOR UPDATE требует explicit intent, так как может привести к очень плохим последствиям, если слишком много использовать
- Код становится самодокументируемым: в случае, когда пишется REPEATEABLE READ + FOR UPDATE программист понимает, что дальше идет unsafe логика с кучей апдейтов

_TODO: Расширьте обоснование._

**5. Масштабируемость**
- READ COMMITTED хорошо масштабируется, так как не блокируется и ничего не делает с таблицей, тем самым не привнося никакого оверхеда
- Локальное использование блокировок не создает bottleneck, т.к. блокируются только строки
- Connection pooling работает эффективно, т.к. транзазакции заверщаются быстрг

_TODO: Расширьте обоснование._

### Альтернативные подходы

#### Подход 1: Использовать SERIALIZABLE везде

**Плюсы:**
- Максимальная корректность
- Не нужно думать о блокировках
- PostgreSQL автоматически обнаруживает конфликты

**Минусы:**
- Значительное снижение производительности (20-50%)
- Требует retry logic во всех операциях
- Высокий процент rollback при нагрузке
- Сложная отладка serialization failures

**Вывод:** Не рекомендуется для high-load систем.

#### Подход 2: Optimistic Locking (версионирование)

**Реализация:**
```sql
ALTER TABLE orders ADD COLUMN version INTEGER DEFAULT 1;

UPDATE orders 
SET status = 'paid', version = version + 1
WHERE id = '...' AND status = 'created' AND version = 1;

-- Проверить ROW_COUNT, если 0 — конфликт
```

**Плюсы:**
- Нет блокировок на чтение
- Хорошая производительность
- Масштабируется горизонтально

**Минусы:**
- Требует изменения схемы БД
- Требует retry logic в приложении
- Может быть много конфликтов при высокой нагрузке

**Вывод:** Хороший подход для распределенных систем.

#### Подход 3: Advisory Locks

**Реализация:**
```sql
BEGIN;
SELECT pg_advisory_xact_lock(hashtext('order_' || order_id));
-- Критическая секция
COMMIT; -- Блокировка автоматически снимается
```

**Плюсы:**
- Гибкий контроль блокировок
- Можно блокировать логические ресурсы
- Работает на любом уровне изоляции

**Минусы:**
- Легко забыть снять блокировку
- Сложнее отлаживать
- Требует дисциплины от разработчиков

**Вывод:** Полезно для специфичных случаев, но не как основной подход.

### Итоговая рекомендация

Для продакшена маркетплейса используйте **гибридный подход**:

1. **Default isolation level:** `READ COMMITTED`
   - Для обычных операций (чтение каталога, просмотр истории)
   - 95% операций

2. **REPEATABLE READ + FOR UPDATE:** для критичных операций
   - Оплата заказа
   - Изменение баланса
   - Резервирование товара
   - 5% операций

3. **Дополнительно:** Optimistic locking для операций с высокой конкурентностью
   - Обновление счетчиков
   - Изменение рейтингов

**Пример кода:**
```python
async def pay_order(order_id: UUID):
    async with db.transaction(isolation='repeatable_read'):
        # Заблокировать заказ
        order = await db.fetch_one(
            "SELECT * FROM orders WHERE id = $1 FOR UPDATE",
            order_id
        )
        
        if order['status'] != 'created':
            raise OrderAlreadyPaidError()
        
        # Обновить статус
        await db.execute(
            "UPDATE orders SET status = 'paid' WHERE id = $1",
            order_id
        )
        
        # Записать в историю
        await db.execute(
            "INSERT INTO order_status_history (order_id, status) VALUES ($1, 'paid')",
            order_id
        )
```

---

## Заключение

_TODO: Напишите краткое заключение о проделанной работе._

В ходе лабораторной работы было изучено:
1. Как избегать проблемы рейс кондишенов в случае работы с маркетплейсом
2. Изучены кейсы использования разных уровней изоляции
3. Изучены возможности масштабируемости таблиц

Основные выводы:
- READ COMMITED не может защитить от рейс кондишенов, поэтому для таких случаев в любом случае приходится как-либо блокировать таблицы
- SERALIZE не является эффективным решением, т.к дает очень большой оверхед. лучше всего использовать READ COMMITED + FOR UPDATE

---

## Приложение: Результаты тестирования

### Тест 1: Демонстрация проблемы (READ COMMITTED)

_TODO: Вставьте скриншоты или вывод команд, демонстрирующие двойную оплату._

```
D:\dblabs\SCofDB_lab_02>cd backend && pytest .\app\tests\test_concurrent_payment_unsafe.py -v -s
app/tests/test_concurrent_payment_unsafe.py::test_concurrent_payment_unsafe_demonstrates_race_condition [DEBUG] order_id=49e85d8d-2288-4ea0-a8b8-fb03e6149497, current_status=created
[DEBUG] order_id=49e85d8d-2288-4ea0-a8b8-fb03e6149497, current_status=created
⚠️ RACE CONDITION DETECTED!
Order 49e85d8d-2288-4ea0-a8b8-fb03e6149497 was paid TWICE:
  - 2026-04-24 22:12:20.240402+00:00: status = paid
  - 2026-04-24 22:12:20.240402+00:00: status = paid
PASSED 
```

### Тест 2: Решение проблемы (REPEATABLE READ + FOR UPDATE)

_TODO: Вставьте скриншоты или вывод команд, демонстрирующие корректную работу._

```
D:\dblabs\SCofDB_lab_02\backend>pytest .\app\tests\test_concurrent_payment_safe.py -v -s
✅ RACE CONDITION PREVENTED!
Order 606bf4f8-a501-476c-987d-3e2f82f6d3f7 was paid only ONCE:
  - 2026-04-24 22:32:44.961148+00:00: status = paid
Second attempt was rejected: (sqlalchemy.dialects.postgresql.asyncpg.Error) <class 'asyncpg.exceptions.SerializationError'>: could not serialize access due to concurrent update
[SQL: SELECT status FROM orders WHERE id = $1 FOR UPDATE]
[parameters: (UUID('606bf4f8-a501-476c-987d-3e2f82f6d3f7'),)]
(Background on this error at: https://sqlalche.me/e/20/dbapi)
PASSED
app/tests/test_concurrent_payment_safe.py::test_concurrent_payment_safe_with_explicit_timing Total elapsed time: 1.13s
Order 6b14dec4-8b50-49d0-9f64-6d1f6763cffc has onlye been paid once.
PASSED
app/tests/test_concurrent_payment_safe.py::test_concurrent_payment_safe_multiple_orders PASSED
```

---
